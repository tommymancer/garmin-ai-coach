# Zero-friction run: `docker compose up -d`
# Put your settings in a .env file next to this one (copy .env.example).
services:
  coach:
    build: .
    env_file: .env
    volumes:
      - coach-data:/data
    restart: unless-stopped

volumes:
  coach-data:
